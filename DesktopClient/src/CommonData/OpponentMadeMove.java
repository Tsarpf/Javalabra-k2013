package CommonData;

/**
 * I've yet to figure out a better way for this. I think I probably should use JSON in this case too
 * even though all this could easily be put into just one string
 * @author Tsarpf
 *
 */
public class OpponentMadeMove
{
	public boolean foSho = true;
}
